package org.data.model.predict.enums;

public enum GameStatus {
    START("START"), IN_PROGRESS("IN_PROGRESS"), GAME_OVER("GAME_OVER"), TIME_EXPIRED("TIME_EXPIRED");

    GameStatus(String status){}
}
